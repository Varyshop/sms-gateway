<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ir_cron_run_campaign_schedules" model="ir.cron">
        <field name="name">Campaign Scheduler: Execute Due Schedules</field>
        <field name="model_id" ref="model_campaign_schedule"/>
        <field name="state">code</field>
        <field name="code">model._cron_run_schedules()</field>
        <field name="interval_number">15</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
    </record>

</odoo>
